sudo /etc/init.d/mysql stop
sudo mysqld_safe --skip-grant-tables &

	mysql
GRANT ALL PRIVILEGES ON * . * TO 'dung123'@'localhost';
FLUSH PRIVILEGES;


 sudo chmod -R 777 ../magento2

sudo php bin/magento setup:upgrade
sudo bin/magento setup:di:compile
sudo php bin/magento c:f

sudo modprobe -r psmouse


//Tao magento 
Tao xong modul 
php bin/magento module:status
composer install

…./setup

install xong vào 
cd /etc/apache2/sites-available/
thay doi duong dan
sudo nano /etc/apache2/sites-available/magento.conf

cp file đã cài hoàn thiện và sửa 
sudo cp file_cp file_moi.conf

vào sites-enabled/
cd ../sites-enabled/
sudo ln -s ../sites-sites-available/ecommage1.local.conf

sửa đúng đường dẫn
sudo gedit ecommage1.local.conf
tạo thêm host
sudo gedit /etc/hosts

sudo /etc/init.d/apache2 restart



///
https://framework.zend.com/manual/1.12/en/zend.db.select.html

